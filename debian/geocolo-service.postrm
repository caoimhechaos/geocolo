#!/bin/sh

# Abort on error (the usual stuff).
set -e

# Now run the rests of the script as generated by
#DEBHELPER#

# Delete geocolo user.
if [ "$1" = "purge" ]
then
	deluser --system _geocolo || true
	delgroup --system _geocolo || true
fi
